plugins {
    id 'java'
}

repositories {
    mavenCentral();
}

dependencies {
    testImplementation 'junit:junit:4.12'
}

version = 1.0
sourceCompatibility = 8

jar {
    manifest {
        attributes 'Main-Class': 'com.reinard.App'
    }
    archiveName 'split-' + project.version + '.jar'
}

